{
  "name": "vayusetu-database",
  "private": true,
  "scripts": {
    "db:validate": "prisma validate",
    "db:format": "prisma format",
    "db:migrate": "prisma migrate dev",
    "db:deploy": "prisma migrate deploy",
    "db:studio": "prisma studio"
  },
  "dependencies": {
    "dotenv": "^17.2.2",
    "prisma": "^7.0.0"
  }
}
